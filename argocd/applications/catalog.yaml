apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: catalog
  namespace: argocd

spec:
  project: retail-store-production

  sources:
    # Helm chart source
    - repoURL: https://github.com/tcollins520/retail-store-v2-platform-engineering.git
      targetRevision: main
      path: helm/catalog

      helm:
        releaseName: catalog
        valueFiles:
          - $values/environments/production/catalog/values.yaml

    # Production values source
    - repoURL: https://github.com/tcollins520/retail-store-v2-gitops.git
      targetRevision: main
      ref: values

  destination:
    server: https://kubernetes.default.svc
    namespace: default

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true
